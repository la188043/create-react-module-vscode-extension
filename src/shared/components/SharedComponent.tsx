import React from 'react';

import styles from './SharedComponent.module..scss';

interface Props {
}

const SharedComponent = (props: Props) => {
  const {} = props;

  return (
  );
};

export default SharedComponent;
